# CMake build output
build/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile

# In-source CMake artifacts
**/CMakeFiles/
**/cmake_install.cmake
**/Makefile

# Compiled binaries and libraries
lib/libblip.a
games/*/bouncer
games/*/serpent
games/*/galactic_defender
games/*/gen_assets
games/*/assets.stamp

# Claude Code
.claude/
**/.claude/

# Rust
target/

# Node
node_modules/

# Local secrets / env
.env
.env.local

# Supabase project URL + anon key — never committed; the GitHub Pages
# deploy writes this from repo secrets (see .github/workflows/pages.yml).
# Copy web/blip_config.example.js to it for local testing against Supabase.
web/blip_config.js

# Playwright output
test-results/
playwright-report/

# Editor / OS
.DS_Store
*.swp
*~
