[package]
name = "brawler"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[features]
# A contact sheet of every pose instead of the game — see draw::draw_gallery.
gallery = []

[dependencies]
macroquad = { workspace = true }
blip = { workspace = true }
# Also a runtime dependency: the themes are synthesised on the device
# rather than shipped as PCM. See blip_assets::brawler::theme_wav.
blip_assets = { workspace = true }

[build-dependencies]
blip_assets = { workspace = true }
